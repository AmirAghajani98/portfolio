module.exports = {
  plugins: [
    "postcss-nested", // Ensure postcss-nested is here for nesting support
    "tailwindcss", // Tailwind CSS plugin
    "autoprefixer", // Autoprefixer plugin if you're using it
  ],
};
